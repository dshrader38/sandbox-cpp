{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb-mi",
            "request": "launch",
            "target": "./bin/cmake-sample",
            "cwd": "${workspaceRoot}"
        }
    ]
}